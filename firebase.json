{
  "hosting": {
    "public": ".",
    "ignore": ["firebase.json", "README.md", "database.rules.json", "run.bat", "*.zip", ".git/**", ".firebase/**", "backups/**"],
    "headers": [
      {
        "source": "**",
        "headers": [{"key": "Cache-Control", "value": "no-store, max-age=0, must-revalidate"}]
      }
    ]
  },
  "database": {
    "rules": "database.rules.json"
  }
}
